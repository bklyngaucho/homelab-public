# Secrets — never commit these
.env
*.env
!.env.template
.homelab.secrets
*.secrets
!*.secrets.template

# hl.py API credentials (use scripts/.hl.env.template as reference)
scripts/.hl.env

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor files
.vscode/
.idea/
*.swp
*.swo

# Ansible
ansible/*.retry
ansible/vault-password

# Logs
*.log

# Health digest cache (live data, regenerated by health-digest.py)
scripts/health_cache.json

# Audit session artifacts (not for repo)
compose/.docker-compose.pre-audit.bak
*-eval-review.html

# Scratch / temp files
*.csv
skills/zir70kI5
